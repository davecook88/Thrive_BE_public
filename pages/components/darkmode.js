import React from 'react'

const DarkMode = () => {
    return (
        <>
            <button>
                Enable Dark Mode
            </button>

            <button>Disable Dark Mode</button>
        </>
    )
}

export default DarkMode
