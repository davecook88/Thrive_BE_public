import React from "react";

const UnauthorizedAccessPage = () => (
  <div className="w-full h-full text-center center-align">
    You don't have access to this page
  </div>
);

export default UnauthorizedAccessPage;
