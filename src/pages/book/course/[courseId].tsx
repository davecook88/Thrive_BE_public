import React from "react";
import StripePayment from "../../../components/payment/stripe/StripePayment";
import BookCoursePage from "../../../components/pages/booking/course/[courseId]";
import { GetStaticProps } from "next";

const BookCourseLandingPage = () => {
  return <BookCourseLandingPage />;
};

// export const GetStaticPaths = () =>

// export const getStaticProps: GetStaticProps = async (context) => {
//   console.log(context);
//   return {
//     props: {}, // will be passed to the page component as props
//   };
// };

export default BookCourseLandingPage;
